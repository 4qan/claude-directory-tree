---
phase: 04-plugin-distribution
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - plugin/.claude-plugin/plugin.json
  - plugin/commands/launch.md
  - plugin/commands/add.md
  - .claude-plugin/marketplace.json
  - package.json
  - LICENSE
autonomous: true
requirements: [DIST-01, DIST-02, DIST-03]

must_haves:
  truths:
    - "Plugin directory structure passes claude plugin validate"
    - "npm pack produces tarball containing dist/, README.md, LICENSE, package.json and nothing else"
    - "prepublishOnly script runs build before publish"
  artifacts:
    - path: "plugin/.claude-plugin/plugin.json"
      provides: "Plugin manifest with name claude-tree, version 0.1.0"
      contains: '"name": "claude-tree"'
    - path: "plugin/commands/launch.md"
      provides: "/claude-tree:launch command prompt"
      contains: "npx claude-directory-tree"
    - path: "plugin/commands/add.md"
      provides: "/claude-tree:add command prompt"
      contains: "npx claude-directory-tree --add"
    - path: ".claude-plugin/marketplace.json"
      provides: "Marketplace catalog pointing to ./plugin"
      contains: '"source": "./plugin"'
    - path: "package.json"
      provides: "npm distribution config with files allowlist and prepublishOnly"
      contains: '"prepublishOnly"'
    - path: "LICENSE"
      provides: "MIT license file"
      contains: "MIT License"
  key_links:
    - from: "plugin/commands/launch.md"
      to: "dist/cli.js"
      via: "npx claude-directory-tree invokes the bin entry"
      pattern: "npx claude-directory-tree"
    - from: ".claude-plugin/marketplace.json"
      to: "plugin/.claude-plugin/plugin.json"
      via: "source field resolves to plugin directory"
      pattern: '"source": "./plugin"'
    - from: "package.json files field"
      to: "dist/"
      via: "files allowlist includes dist/ for npm tarball"
      pattern: '"dist/"'
---

<objective>
Create the Claude Code plugin structure and configure npm package for distribution.

Purpose: Make the app installable as a Claude Code plugin (via marketplace) and publishable to npm (via `npm publish`). All application logic exists; this is purely structural/config work.

Output: Plugin directory with manifest and command files, marketplace.json at repo root, updated package.json with `files` and `prepublishOnly`, LICENSE file.
</objective>

<execution_context>
@/Users/furqantariq/.claude/get-shit-done/workflows/execute-plan.md
@/Users/furqantariq/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-plugin-distribution/04-RESEARCH.md
@.planning/phases/04-plugin-distribution/04-CONTEXT.md

<interfaces>
<!-- Existing CLI entry point that plugin commands invoke via npx -->

From src/cli.ts:
```typescript
// Parse args: supports [targetDir] and --add <path>
// npx claude-directory-tree [dir]       → scan dir, start server, open browser
// npx claude-directory-tree --add <path> → register project, exit
```

From package.json:
```json
{
  "name": "claude-directory-tree",
  "version": "0.1.0",
  "bin": { "claude-directory-tree": "./dist/cli.js" },
  "scripts": {
    "build:server": "tsup",
    "build:client": "vite build",
    "build": "npm run build:server && npm run build:client"
  }
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Claude Code plugin structure and marketplace manifest</name>
  <files>
    plugin/.claude-plugin/plugin.json,
    plugin/commands/launch.md,
    plugin/commands/add.md,
    .claude-plugin/marketplace.json
  </files>
  <read_first>
    .planning/phases/04-plugin-distribution/04-RESEARCH.md,
    src/cli.ts,
    package.json
  </read_first>
  <action>
Create the plugin directory structure at the repo root:

1. Create `plugin/.claude-plugin/plugin.json` with this exact content:
```json
{
  "name": "claude-tree",
  "version": "0.1.0",
  "description": "Visual explorer for all Claude Code artifacts across projects and scopes",
  "author": {
    "name": "Furqan Tariq",
    "url": "https://github.com/furqantariq"
  },
  "homepage": "https://github.com/furqantariq/claude-directory-tree",
  "repository": "https://github.com/furqantariq/claude-directory-tree",
  "license": "MIT",
  "keywords": ["claude", "artifacts", "tree", "explorer"]
}
```

2. Create `plugin/commands/launch.md` with this exact content:
```markdown
---
description: Launch Claude Directory Tree - visual explorer for Claude artifacts
---

Run the following command to launch the Claude Directory Tree UI:

\`\`\`bash
npx claude-directory-tree $ARGUMENTS
\`\`\`

This opens a local web app that shows all your Claude artifacts (skills, agents, commands, plugins, hooks, CLAUDE.md files) across all projects in a visual tree.
```

3. Create `plugin/commands/add.md` with this exact content:
```markdown
---
description: Register a project with Claude Directory Tree without opening the UI
---

Run the following command to register the current project (or a specified path) with Claude Directory Tree:

\`\`\`bash
npx claude-directory-tree --add $ARGUMENTS
\`\`\`

If no path is specified, registers the current directory. The project will appear in the tree next time you launch Claude Directory Tree.
```

4. Create `.claude-plugin/marketplace.json` at the repo root (NOT inside plugin/) with this exact content:
```json
{
  "name": "claude-directory-tree",
  "owner": {
    "name": "Furqan Tariq"
  },
  "plugins": [
    {
      "name": "claude-tree",
      "source": "./plugin",
      "description": "Visual explorer for all Claude Code artifacts across projects and scopes",
      "version": "0.1.0",
      "homepage": "https://github.com/furqantariq/claude-directory-tree"
    }
  ]
}
```

CRITICAL: `commands/` directory must be at plugin root (`plugin/commands/`), NOT inside `plugin/.claude-plugin/`. Only `plugin.json` goes in `.claude-plugin/`.

Plugin name is `claude-tree`, so commands will be `/claude-tree:launch` and `/claude-tree:add`.
  </action>
  <verify>
    <automated>cd /Users/furqantariq/Documents/Projects/Claude\ Directory\ Tree && test -f plugin/.claude-plugin/plugin.json && test -f plugin/commands/launch.md && test -f plugin/commands/add.md && test -f .claude-plugin/marketplace.json && echo "All plugin files exist"</automated>
  </verify>
  <acceptance_criteria>
    - plugin/.claude-plugin/plugin.json contains '"name": "claude-tree"'
    - plugin/.claude-plugin/plugin.json contains '"version": "0.1.0"'
    - plugin/commands/launch.md contains 'npx claude-directory-tree $ARGUMENTS'
    - plugin/commands/launch.md frontmatter contains 'description: Launch Claude Directory Tree'
    - plugin/commands/add.md contains 'npx claude-directory-tree --add $ARGUMENTS'
    - plugin/commands/add.md frontmatter contains 'description: Register a project'
    - .claude-plugin/marketplace.json contains '"source": "./plugin"'
    - .claude-plugin/marketplace.json contains '"name": "claude-directory-tree"' (marketplace name)
    - .claude-plugin/marketplace.json plugins array contains '"name": "claude-tree"' (plugin name)
    - commands/ directory is at plugin/commands/, NOT at plugin/.claude-plugin/commands/
  </acceptance_criteria>
  <done>Plugin directory structure exists with correct manifest, two command files, and marketplace catalog. Commands will resolve to /claude-tree:launch and /claude-tree:add.</done>
</task>

<task type="auto">
  <name>Task 2: Configure npm package for distribution and create LICENSE</name>
  <files>
    package.json,
    LICENSE
  </files>
  <read_first>
    package.json,
    .gitignore,
    tsup.config.ts
  </read_first>
  <action>
1. Update `package.json` to add the following fields (preserve all existing fields):

Add `"files"` field (top-level, after `"bin"`):
```json
"files": [
  "dist/",
  "README.md",
  "LICENSE"
]
```

Add `"prepublishOnly"` to the `"scripts"` object:
```json
"prepublishOnly": "npm run build"
```

Add `"repository"` field (top-level):
```json
"repository": {
  "type": "git",
  "url": "https://github.com/furqantariq/claude-directory-tree.git"
}
```

Add `"homepage"` field:
```json
"homepage": "https://github.com/furqantariq/claude-directory-tree"
```

Add `"author"` field:
```json
"author": "Furqan Tariq"
```

Do NOT include `plugin/` in the `files` array. The plugin is distributed via git (marketplace), not npm.

2. Create `LICENSE` file at repo root with MIT License text:
- Copyright (c) 2026 Furqan Tariq
- Full MIT license body

3. Verify `dist/` is in `.gitignore` (it already is). This is correct: npm builds fresh via prepublishOnly, and `files` field overrides `.gitignore` for the tarball.

4. Run `npm test` to confirm existing tests still pass after package.json changes.

5. Run `npm run build && npm pack` to verify the tarball contains the expected files. Inspect with `tar -tf claude-directory-tree-0.1.0.tgz`. Expected contents: dist/cli.js, dist/server/*, README.md (will be missing until Plan 02, that's fine), LICENSE, package.json.
  </action>
  <verify>
    <automated>cd /Users/furqantariq/Documents/Projects/Claude\ Directory\ Tree && npm test && npm run build && npm pack --dry-run 2>&1 | head -40</automated>
  </verify>
  <acceptance_criteria>
    - package.json contains '"files"' with '"dist/"', '"README.md"', '"LICENSE"'
    - package.json contains '"prepublishOnly": "npm run build"'
    - package.json contains '"repository"'
    - package.json contains '"homepage"'
    - package.json contains '"author": "Furqan Tariq"'
    - package.json does NOT contain 'plugin/' in the files array
    - LICENSE file exists at repo root and contains 'MIT License'
    - LICENSE contains 'Furqan Tariq'
    - npm test exits 0
    - npm pack --dry-run lists dist/ files and LICENSE
  </acceptance_criteria>
  <done>package.json configured for npm publish with files allowlist and prepublishOnly. LICENSE file exists. npm pack produces correct tarball contents. Existing tests pass.</done>
</task>

</tasks>

<verification>
- All plugin files exist in correct locations (plugin/.claude-plugin/plugin.json, plugin/commands/*.md, .claude-plugin/marketplace.json)
- package.json has files, prepublishOnly, repository, homepage, author fields
- LICENSE exists with MIT text
- `npm test` passes
- `npm pack --dry-run` shows dist/, LICENSE, package.json (README.md will be added in Plan 02)
</verification>

<success_criteria>
- Plugin structure is complete and correctly organized
- npm package is configured for clean publishing
- No application behavior changed (all existing tests pass)
</success_criteria>

<output>
After completion, create `.planning/phases/04-plugin-distribution/04-01-SUMMARY.md`
</output>
