---
phase: 04-plugin-distribution
plan: 02
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - README.md
autonomous: false
requirements: [DIST-01, DIST-02, DIST-03]

must_haves:
  truths:
    - "README explains what the tool does with a clear value proposition"
    - "README has install instructions for both plugin and npx paths"
    - "README documents available commands and CLI flags"
  artifacts:
    - path: "README.md"
      provides: "Project documentation with install, usage, and contribution info"
      min_lines: 80
      contains: "npx claude-directory-tree"
  key_links:
    - from: "README.md plugin install section"
      to: ".claude-plugin/marketplace.json"
      via: "Documents the marketplace add command"
      pattern: "plugin marketplace add"
    - from: "README.md npx section"
      to: "package.json bin field"
      via: "Documents the npx command"
      pattern: "npx claude-directory-tree"
---

<objective>
Create the project README and verify the complete distribution package.

Purpose: Give users clear documentation on what the tool does and how to install it via either distribution channel. Then verify the full package end-to-end.

Output: README.md with feature overview, install instructions, usage, and contributing section. Human verification of plugin structure.
</objective>

<execution_context>
@/Users/furqantariq/.claude/get-shit-done/workflows/execute-plan.md
@/Users/furqantariq/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-plugin-distribution/04-CONTEXT.md
@.planning/phases/04-plugin-distribution/04-RESEARCH.md
@.planning/phases/04-plugin-distribution/04-01-SUMMARY.md

<interfaces>
<!-- From Plan 01 outputs -->
From package.json:
```json
{
  "name": "claude-directory-tree",
  "version": "0.1.0",
  "bin": { "claude-directory-tree": "./dist/cli.js" }
}
```

Plugin commands:
- /claude-tree:launch - runs npx claude-directory-tree [dir]
- /claude-tree:add - runs npx claude-directory-tree --add [path]

CLI flags:
- [dir] - optional target directory (default: cwd)
- --add <path> - register a project path
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create README.md</name>
  <files>README.md</files>
  <read_first>
    package.json,
    plugin/.claude-plugin/plugin.json,
    plugin/commands/launch.md,
    plugin/commands/add.md,
    .claude-plugin/marketplace.json,
    src/cli.ts,
    .planning/phases/04-plugin-distribution/04-CONTEXT.md
  </read_first>
  <action>
Create `README.md` at the repo root with the following sections in order:

**1. Title and tagline:**
```
# Claude Directory Tree

Visual explorer for all your Claude Code artifacts across projects and scopes.
```

**2. What it does** (3-5 bullet points):
- See all Claude artifacts (skills, agents, commands, plugins, hooks, CLAUDE.md, MCP servers, memory) in one tree view
- Organize by scope (global vs project) or filesystem hierarchy
- Copy, move, promote (project to global), and demote (global to project) artifacts
- Search and filter by name or type
- Toggle plugins on/off directly from the UI

**3. Install section** with two subsections:

**As a Claude Code Plugin:**
```bash
# Add the marketplace
/plugin marketplace add furqantariq/claude-directory-tree

# Install the plugin
/plugin install claude-tree@claude-directory-tree
```

Then use `/claude-tree:launch` to open the UI or `/claude-tree:add [path]` to register a project.

**As a standalone CLI:**
```bash
npx claude-directory-tree
```

Or install globally:
```bash
npm install -g claude-directory-tree
claude-directory-tree
```

**4. Usage section:**

```bash
# Launch from current directory
npx claude-directory-tree

# Launch from a specific directory
npx claude-directory-tree /path/to/projects

# Register a project for the universal view
npx claude-directory-tree --add /path/to/project
```

**5. Features section** - brief list:
- Flat scope view and directory hierarchy view with toggle
- Context menu with copy, move, promote, demote actions
- Conflict detection with overwrite prompt
- Detail panel showing artifact summaries
- Dark mode
- Local only, no network calls, no telemetry

**6. Requirements:**
- Node.js >= 20.17.0
- Claude Code >= 1.0.33 (for plugin install)

**7. Development section:**
```bash
git clone https://github.com/furqantariq/claude-directory-tree.git
cd claude-directory-tree
npm install
npm run build
npm test
```

**8. License:**
MIT

Note: The user mentioned wanting screenshots/GIFs in the README. Add placeholder comments like `<!-- TODO: Add screenshot of tree view -->` at appropriate places (after title, in features section). The user will add actual images later.

Keep the tone direct and practical. No marketing fluff.
  </action>
  <verify>
    <automated>cd /Users/furqantariq/Documents/Projects/Claude\ Directory\ Tree && test -f README.md && wc -l README.md | awk '{print ($1 >= 80) ? "README has " $1 " lines (OK)" : "README too short: " $1 " lines"}'</automated>
  </verify>
  <acceptance_criteria>
    - README.md exists at repo root
    - README.md contains '# Claude Directory Tree'
    - README.md contains 'npx claude-directory-tree'
    - README.md contains '/plugin marketplace add furqantariq/claude-directory-tree'
    - README.md contains '/plugin install claude-tree@claude-directory-tree'
    - README.md contains '/claude-tree:launch'
    - README.md contains '/claude-tree:add'
    - README.md contains '--add'
    - README.md contains 'Node.js >= 20.17.0'
    - README.md contains 'MIT'
    - README.md contains screenshot placeholder comments (<!-- TODO)
    - README.md is at least 80 lines
  </acceptance_criteria>
  <done>README.md exists with install instructions for both plugin and npx paths, usage examples, feature list, dev setup, and license. Screenshot placeholders marked for user to fill in later.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify distribution package</name>
  <files>README.md, plugin/.claude-plugin/plugin.json, .claude-plugin/marketplace.json, package.json, LICENSE</files>
  <action>
Run `npm run build && npm pack` and present the tarball contents to the user. Run `tar -tf claude-directory-tree-0.1.0.tgz` to list files. Present the full list of distribution artifacts for review:
- Plugin structure (plugin/ directory)
- npm tarball contents
- README.md
  </action>
  <verify>
    <automated>cd /Users/furqantariq/Documents/Projects/Claude\ Directory\ Tree && npm run build && npm pack --dry-run 2>&1 | tail -20</automated>
  </verify>
  <done>User has reviewed and approved the complete distribution package.</done>
  <what-built>
Complete distribution package: Claude Code plugin structure (plugin/ directory with manifest and commands), marketplace.json, npm package configuration (files, prepublishOnly, LICENSE), and README.
  </what-built>
  <how-to-verify>
1. Review the plugin structure:
   - `plugin/.claude-plugin/plugin.json` has correct metadata
   - `plugin/commands/launch.md` and `plugin/commands/add.md` have correct prompts
   - `.claude-plugin/marketplace.json` at repo root points to `./plugin`

2. Review npm package:
   - Run `npm run build && npm pack` and inspect the tarball: `tar -tf claude-directory-tree-0.1.0.tgz`
   - Verify it contains: dist/cli.js, dist/server/*, README.md, LICENSE, package.json
   - Verify it does NOT contain: src/, plugin/, node_modules/, tests/

3. Review README.md for accuracy and tone.

4. (Optional) Test plugin locally:
   - In Claude Code, run with `--plugin-dir /path/to/repo/plugin` flag
   - Try `/claude-tree:launch`
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues to fix</resume-signal>
</task>

</tasks>

<verification>
- README.md exists with all required sections
- `npm pack` tarball contains only dist/, README.md, LICENSE, package.json
- Plugin files are correctly structured
- Human has reviewed the complete distribution package
</verification>

<success_criteria>
- README provides clear install and usage instructions for both distribution channels
- User has verified the package contents and plugin structure
- Project is ready for `npm publish` and GitHub push
</success_criteria>

<output>
After completion, create `.planning/phases/04-plugin-distribution/04-02-SUMMARY.md`
</output>
