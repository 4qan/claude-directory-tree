---
phase: 01-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - tsconfig.json
  - tsup.config.ts
  - vitest.config.ts
  - vite.config.ts
  - src/scanner/types.ts
  - src/server/types.ts
  - tests/fixtures/.claude/commands/hello.md
  - tests/fixtures/.claude/agents/helper.md
  - tests/fixtures/.claude/skills/coding.md
  - tests/fixtures/.claude/memory/context.md
  - tests/fixtures/.claude/plans/sprint.md
  - tests/fixtures/.claude/settings.json
  - tests/fixtures/.claude/CLAUDE.md
  - tests/fixtures/.claude/.mcp.json
  - tests/fixtures/nested/project-a/.claude/commands/deploy.md
  - tests/fixtures/nested/project-b/.claude/agents/reviewer.md
  - .gitignore
autonomous: true
requirements:
  - INFRA-01
  - INFRA-04
must_haves:
  truths:
    - "Project builds successfully with tsup (server) and vite (client)"
    - "All type schemas are defined and exported before any implementation begins"
    - "Test fixtures exist representing all artifact types for scanner tests"
  artifacts:
    - path: "package.json"
      provides: "npm package config with bin field, dependencies, scripts"
      contains: "claude-directory-tree"
    - path: "src/scanner/types.ts"
      provides: "Zod schemas and TypeScript types for artifact tree"
      exports: ["ArtifactTypeSchema", "ArtifactSchema", "ScopeNodeSchema", "ScanResponseSchema"]
    - path: "tsup.config.ts"
      provides: "Server build configuration"
      contains: "defineConfig"
    - path: "vitest.config.ts"
      provides: "Test framework configuration"
      contains: "defineConfig"
  key_links:
    - from: "package.json"
      to: "dist/cli.js"
      via: "bin field"
      pattern: '"claude-directory-tree".*"dist/cli.js"'
    - from: "tsup.config.ts"
      to: "src/cli.ts"
      via: "entry array"
      pattern: "src/cli.ts"
---

<objective>
Scaffold the greenfield project: package.json, TypeScript configs, build tooling, type contracts, test framework, and fixture data.

Purpose: Every subsequent plan depends on the project existing with correct dependencies, build pipeline, and shared type definitions. This is the foundation layer.
Output: Buildable project skeleton with all types defined, test fixtures created, and build tooling configured.
</objective>

<execution_context>
@/Users/furqantariq/.claude/get-shit-done/workflows/execute-plan.md
@/Users/furqantariq/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Initialize project with package.json, TypeScript, and build tooling</name>
  <files>package.json, tsconfig.json, tsup.config.ts, vitest.config.ts, vite.config.ts, .gitignore, src/cli.ts</files>
  <read_first>.planning/phases/01-foundation/01-RESEARCH.md</read_first>
  <action>
Create the project from scratch. All files at project root `/Users/furqantariq/Documents/Projects/Claude Directory Tree/`.

**package.json:**
```json
{
  "name": "claude-directory-tree",
  "version": "0.1.0",
  "description": "Visual file-explorer for all your Claude Code artifacts across projects and scopes",
  "type": "module",
  "bin": {
    "claude-directory-tree": "./dist/cli.js"
  },
  "engines": {
    "node": ">=20.17.0"
  },
  "scripts": {
    "build:server": "tsup",
    "build:client": "vite build",
    "build": "npm run build:server && npm run build:client",
    "dev:server": "tsup --watch",
    "dev:client": "vite",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "keywords": ["claude", "claude-code", "artifacts", "directory-tree"],
  "license": "MIT"
}
```

Run `npm install` with these dependencies:
- Runtime: `fastify@^5.8.0 @fastify/static@^9.0.0 @fastify/cors@^11.2.0 fastify-type-provider-zod@^6.1.0 zod@^4.3.0 gray-matter@^4.0.3 get-port@^7.2.0 open@^11.0.0 write-file-atomic@^7.0.1`
- Dev: `tsup@^8.5.0 typescript@latest vite@^8.0.0 @vitejs/plugin-react@^6.0.0 vitest@latest react@^19.0.0 react-dom@^19.0.0 @types/react@latest @types/react-dom@latest @types/write-file-atomic@latest`

**tsconfig.json:**
```json
{
  "compilerOptions": {
    "target": "ES2022",
    "module": "ESNext",
    "moduleResolution": "bundler",
    "esModuleInterop": true,
    "strict": true,
    "skipLibCheck": true,
    "outDir": "dist",
    "rootDir": "src",
    "declaration": false,
    "sourceMap": false,
    "resolveJsonModule": true,
    "jsx": "react-jsx"
  },
  "include": ["src"],
  "exclude": ["node_modules", "dist", "client"]
}
```

**tsup.config.ts:**
Two separate entry configs to handle shebang only on cli.ts:
```typescript
import { defineConfig } from 'tsup';

export default defineConfig([
  {
    entry: ['src/cli.ts'],
    format: ['esm'],
    outDir: 'dist',
    target: 'node20',
    clean: true,
    banner: { js: '#!/usr/bin/env node' },
  },
  {
    entry: ['src/server/index.ts'],
    format: ['esm'],
    outDir: 'dist/server',
    target: 'node20',
    clean: false,
  },
]);
```

**vitest.config.ts:**
```typescript
import { defineConfig } from 'vitest/config';

export default defineConfig({
  test: {
    globals: true,
    environment: 'node',
  },
});
```

**vite.config.ts:**
```typescript
import { defineConfig } from 'vite';
import react from '@vitejs/plugin-react';

export default defineConfig({
  plugins: [react()],
  root: 'client',
  build: {
    outDir: 'dist',
    emptyOutDir: true,
  },
});
```

**src/cli.ts** (stub for build verification):
```typescript
#!/usr/bin/env node
console.log('claude-directory-tree: stub');
```

**.gitignore:**
```
node_modules/
dist/
client/dist/
*.tsbuildinfo
.DS_Store
```
  </action>
  <verify>
    <automated>cd "/Users/furqantariq/Documents/Projects/Claude Directory Tree" && npm run build:server 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - package.json contains `"name": "claude-directory-tree"`
    - package.json contains `"type": "module"`
    - package.json contains `"claude-directory-tree": "./dist/cli.js"` in bin field
    - package.json contains `"node": ">=20.17.0"` in engines
    - tsup.config.ts contains `entry: ['src/cli.ts']`
    - tsup.config.ts contains `banner: { js: '#!/usr/bin/env node' }` only for cli entry
    - tsup.config.ts contains second config for `src/server/index.ts` without banner
    - vitest.config.ts contains `environment: 'node'`
    - vite.config.ts contains `root: 'client'`
    - `npm run build:server` exits 0 and produces dist/cli.js with shebang
    - dist/cli.js first line is `#!/usr/bin/env node`
    - node_modules/ exists with all dependencies installed
  </acceptance_criteria>
  <done>Project skeleton builds successfully. tsup produces dist/cli.js with shebang. All dependencies installed.</done>
</task>

<task type="auto">
  <name>Task 2: Define type contracts and create test fixtures</name>
  <files>src/scanner/types.ts, src/server/types.ts, tests/fixtures/.claude/commands/hello.md, tests/fixtures/.claude/agents/helper.md, tests/fixtures/.claude/skills/coding.md, tests/fixtures/.claude/memory/context.md, tests/fixtures/.claude/plans/sprint.md, tests/fixtures/.claude/settings.json, tests/fixtures/.claude/CLAUDE.md, tests/fixtures/.claude/.mcp.json, tests/fixtures/nested/project-a/.claude/commands/deploy.md, tests/fixtures/nested/project-b/.claude/agents/reviewer.md</files>
  <read_first>.planning/phases/01-foundation/01-RESEARCH.md</read_first>
  <action>
**src/scanner/types.ts** — Zod schemas and inferred TypeScript types for the entire artifact tree API:

```typescript
import { z } from 'zod';

export const ArtifactTypeSchema = z.enum([
  'command', 'agent', 'skill', 'hook', 'claude-md',
  'mcp-config', 'memory', 'plan', 'plugin', 'unknown'
]);
export type ArtifactType = z.infer<typeof ArtifactTypeSchema>;

export const ArtifactSchema = z.object({
  id: z.string(),
  name: z.string(),
  type: ArtifactTypeSchema,
  absolutePath: z.string(),
  relativePath: z.string(),
  scope: z.enum(['global', 'project']),
  projectId: z.string(),
  frontmatter: z.record(z.unknown()).optional(),
  children: z.array(z.lazy(() => ArtifactSchema)).optional(),
});
export type Artifact = z.infer<typeof ArtifactSchema>;

export const ScopeNodeSchema = z.object({
  id: z.string(),
  label: z.string(),
  scope: z.enum(['global', 'project']),
  rootPath: z.string(),
  artifacts: z.array(ArtifactSchema),
  artifactCount: z.number(),
});
export type ScopeNode = z.infer<typeof ScopeNodeSchema>;

export const ScanResponseSchema = z.object({
  scannedAt: z.string(),
  targetDir: z.string(),
  scopes: z.array(ScopeNodeSchema),
  totalArtifacts: z.number(),
});
export type ScanResponse = z.infer<typeof ScanResponseSchema>;
```

**src/server/types.ts** — Server startup options type:
```typescript
import type { ScanResponse } from '../scanner/types.js';

export interface ServerOptions {
  port: number;
  scanResult: ScanResponse;
}
```

**Test fixtures** — Create a directory structure that covers all artifact types. Each `.md` file should have minimal frontmatter:

`tests/fixtures/.claude/commands/hello.md`:
```markdown
---
type: command
name: hello
---
A test command.
```

`tests/fixtures/.claude/agents/helper.md`:
```markdown
---
type: agent
name: helper
---
A test agent.
```

`tests/fixtures/.claude/skills/coding.md`:
```markdown
---
type: skill
name: coding
---
A test skill.
```

`tests/fixtures/.claude/memory/context.md`:
```markdown
---
name: context
---
A test memory file.
```

`tests/fixtures/.claude/plans/sprint.md`:
```markdown
---
name: sprint
---
A test plan file.
```

`tests/fixtures/.claude/settings.json`:
```json
{
  "hooks": [
    { "name": "pre-commit", "command": "npm test" },
    { "name": "post-push", "command": "echo done" }
  ]
}
```

`tests/fixtures/.claude/CLAUDE.md`:
```markdown
# Project Instructions
Test CLAUDE.md file.
```

`tests/fixtures/.claude/.mcp.json`:
```json
{
  "mcpServers": {
    "test-server": {
      "command": "node",
      "args": ["server.js"]
    }
  }
}
```

**Nested project fixtures** for testing recursive discovery (SCAN-01):

`tests/fixtures/nested/project-a/.claude/commands/deploy.md`:
```markdown
---
type: command
name: deploy
---
Deploy command.
```

`tests/fixtures/nested/project-b/.claude/agents/reviewer.md`:
```markdown
---
type: agent
name: reviewer
---
Code reviewer agent.
```
  </action>
  <verify>
    <automated>cd "/Users/furqantariq/Documents/Projects/Claude Directory Tree" && npx tsx -e "import { ScanResponseSchema } from './src/scanner/types.js'; console.log('types OK:', ScanResponseSchema.shape.scopes !== undefined)" 2>&1</automated>
  </verify>
  <acceptance_criteria>
    - src/scanner/types.ts exports ArtifactTypeSchema with enum values: command, agent, skill, hook, claude-md, mcp-config, memory, plan, plugin, unknown
    - src/scanner/types.ts exports ArtifactSchema with fields: id, name, type, absolutePath, relativePath, scope, projectId, frontmatter, children
    - src/scanner/types.ts exports ScopeNodeSchema with fields: id, label, scope, rootPath, artifacts, artifactCount
    - src/scanner/types.ts exports ScanResponseSchema with fields: scannedAt, targetDir, scopes, totalArtifacts
    - src/server/types.ts exports ServerOptions interface with port and scanResult fields
    - tests/fixtures/.claude/commands/hello.md contains `type: command`
    - tests/fixtures/.claude/agents/helper.md contains `type: agent`
    - tests/fixtures/.claude/skills/coding.md contains `type: skill`
    - tests/fixtures/.claude/settings.json contains `"hooks"` array with 2 entries
    - tests/fixtures/.claude/CLAUDE.md exists
    - tests/fixtures/.claude/.mcp.json contains `"mcpServers"`
    - tests/fixtures/nested/project-a/.claude/commands/deploy.md exists
    - tests/fixtures/nested/project-b/.claude/agents/reviewer.md exists
  </acceptance_criteria>
  <done>All Zod schemas compile and export correctly. Test fixtures cover all 8+ artifact types plus nested project structure for discovery testing.</done>
</task>

</tasks>

<verification>
- `npm run build:server` exits 0
- `dist/cli.js` starts with `#!/usr/bin/env node`
- Types import without errors: `npx tsx -e "import * as t from './src/scanner/types.js'; console.log(Object.keys(t))"`
- Fixture directory has files for all artifact types: commands, agents, skills, memory, plans, hooks (settings.json), CLAUDE.md, .mcp.json
</verification>

<success_criteria>
- Buildable TypeScript project with ESM output
- All shared type contracts defined and exported
- Test fixture directory covering every artifact type
- Build tooling (tsup, vite, vitest) configured and functional
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-01-SUMMARY.md`
</output>
