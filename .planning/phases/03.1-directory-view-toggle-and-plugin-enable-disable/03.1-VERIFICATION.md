---
phase: 03.1-directory-view-toggle-and-plugin-enable-disable
verified: 2026-03-30T23:05:00Z
status: gaps_found
score: 9/10 must-haves verified
re_verification: false
gaps:
  - truth: "User can enable/disable a plugin via context menu action (first action before Open in Editor)"
    status: failed
    reason: "The plugin toggle action renders AFTER Open in Editor, Show in Folder, and Copy Path in the context menu (lines 229-253 of ContextMenu.tsx). The must_have truth in Plan 02 and the task action both explicitly require it to appear FIRST, before Open in Editor."
    artifacts:
      - path: "client/src/components/ContextMenu.tsx"
        issue: "Toggle plugin MenuItemRow appears at line ~247, after ExternalLink (Open in Editor) at line 229"
    missing:
      - "Move the plugin toggle MenuItemRow block (lines 247-254) to be the first child inside the leaf menu JSX, before the ExternalLink Open in Editor row"
human_verification:
  - test: "View toggle visual correctness"
    expected: "Two-icon toggle (List/FolderTree) visible in tree bar. Clicking FolderTree switches to filesystem hierarchy. Non-Claude folders show Folder icon with muted text. Current project folder shows blue 'current' badge. Refreshing page preserves last selected view."
    why_human: "Visual appearance and interactive behavior cannot be verified programmatically."
  - test: "Directory view hierarchy structure"
    expected: "Global scope appears at top as separate root. Projects nest under shared filesystem path (e.g. Documents > Projects > ProjectA > .claude > commands > foo.md). Switching back to flat view restores original scope grouping."
    why_human: "Requires runtime rendering of tree data to verify visual hierarchy."
  - test: "Plugin toggle end-to-end flow"
    expected: "Right-clicking a plugin shows toggle action in context menu. Clicking it shows toast ('Plugin enabled' or 'Plugin disabled'), tree refreshes, enabled/disabled badge updates on the plugin artifact. Same flow works from detail panel button."
    why_human: "Requires running dev server with actual settings.json fixture to observe toast and tree refresh."
---

# Phase 3.1: Directory View Toggle and Plugin Enable/Disable Verification Report

**Phase Goal:** Users can switch between flat scope view and filesystem hierarchy view, and toggle plugins on/off directly from the tree UI
**Verified:** 2026-03-30T23:05:00Z
**Status:** gaps_found
**Re-verification:** No — initial verification

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | buildDirectoryItemMaps produces a filesystem-path trie from ScopeNode[] with folder nodes for ancestor directories | VERIFIED | File exists at `client/src/lib/buildDirectoryItemMaps.ts`, 277 lines, substantive implementation. All 8 unit tests pass. |
| 2 | Global scope renders as a separate root, not inline at filesystem position | VERIFIED | Test 2 passes: `children['root'][0]` === `'global'`, and global is not placed under the filesystem ancestor. |
| 3 | Common ancestor computed only across project scopes (never includes global path) | VERIFIED | Implementation separates `globalScopes` and `projectScopes` before computing `commonAncestor(projectDirs)`. Test confirms global at index 0 of root children. |
| 4 | POST /api/operations/toggle-plugin enables a plugin by adding to enabledPlugins array | VERIFIED | Route exists in `src/server/routes/operations.ts`. Integration test passes: "enable=true adds pluginName to enabledPlugins". |
| 5 | POST /api/operations/toggle-plugin disables a plugin by removing from enabledPlugins array | VERIFIED | Integration test passes: "enable=false removes pluginName from enabledPlugins". |
| 6 | POST /api/operations/toggle-plugin returns 400 when enabledPlugins field is absent | VERIFIED | Integration test passes: "returns 400 with error when settings.json has no enabledPlugins key". |
| 7 | POST /api/operations/toggle-plugin returns 400 when settings.json is malformed JSON | VERIFIED | Integration test passes: "returns 400 with error when settings.json is malformed JSON". |
| 8 | User can toggle between flat view and directory-hierarchy view via a two-segment icon toggle in the tree bar | VERIFIED | `ArtifactTree.tsx` contains `aria-label="Flat view"` and `aria-label="Directory view"` buttons, `viewMode` state, conditional `buildItemMaps`/`buildDirectoryItemMaps` dispatch. Build passes. |
| 9 | View preference persists in localStorage under key 'cdt_view_mode' | VERIFIED | `localStorage.getItem('cdt_view_mode')` in initial state, `localStorage.setItem('cdt_view_mode', mode)` on toggle. |
| 10 | User can enable/disable a plugin via context menu action (first action before Open in Editor) | FAILED | Plugin toggle action renders AFTER Open in Editor, Show in Folder, and Copy Path. Plan 02 must_have truth and task action both require it first. |

**Score:** 9/10 truths verified

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `client/src/lib/buildDirectoryItemMaps.ts` | Directory view tree builder function | VERIFIED | 277 lines, exports `buildDirectoryItemMaps`, imports `ScopeNode`/`Artifact` from types |
| `tests/buildDirectoryItemMaps.test.ts` | Unit tests, min 80 lines | VERIFIED | 273 lines, 8 test cases, all pass |
| `src/shared/operationTypes.ts` | TogglePluginRequestSchema, TogglePluginResultSchema | VERIFIED | Both schemas present at lines 91-104, types exported |
| `src/server/routes/operations.ts` | POST /api/operations/toggle-plugin route | VERIFIED | Route present, reads file, parses JSON, mutates enabledPlugins, writes atomically, returns 400 for all error cases |
| `client/src/lib/operationsApi.ts` | togglePlugin client API function | VERIFIED | `export async function togglePlugin` at line 148, posts to `/api/operations/toggle-plugin` |
| `client/src/components/tree/ArtifactTree.tsx` | View toggle button, viewMode state, localStorage, conditional buildItemMaps/buildDirectoryItemMaps | VERIFIED | All required elements present, Vite build passes |
| `client/src/components/tree/TreeItem.tsx` | Folder node rendering with Folder icon, current badge | VERIFIED | `nodeKind === 'folder'` branch at line 79, imports `Folder` from lucide-react, `hover:bg-muted/50` class |
| `client/src/components/ContextMenu.tsx` | Enable/Disable plugin action for plugin leaves | PARTIAL | Action present and functional, but ordering is wrong (not first before Open in Editor) |
| `client/src/components/ArtifactDetailPanel.tsx` | Enable/Disable button for plugin artifacts | VERIFIED | `handleTogglePlugin` handler, conditional button with 'Disable plugin'/'Enable plugin' label, calls `togglePlugin` |

### Key Link Verification

| From | To | Via | Status | Details |
|------|----|-----|--------|---------|
| `client/src/lib/buildDirectoryItemMaps.ts` | `client/src/lib/types.ts` | imports ScopeNode, Artifact | WIRED | `import type { ScopeNode, Artifact } from './types'` on line 1 |
| `src/server/routes/operations.ts` | `src/shared/operationTypes.ts` | imports TogglePluginRequestSchema | WIRED | `TogglePluginRequestSchema` present in route schema config |
| `client/src/lib/operationsApi.ts` | `/api/operations/toggle-plugin` | fetch POST | WIRED | `fetch('/api/operations/toggle-plugin', { method: 'POST', ... })` |
| `client/src/components/tree/ArtifactTree.tsx` | `client/src/lib/buildDirectoryItemMaps.ts` | import buildDirectoryItemMaps | WIRED | `import { buildDirectoryItemMaps } from '@/lib/buildDirectoryItemMaps'` line 12 |
| `client/src/components/tree/ArtifactTree.tsx` | localStorage | getItem/setItem cdt_view_mode | WIRED | Both `localStorage.getItem('cdt_view_mode')` and `localStorage.setItem('cdt_view_mode', mode)` present |
| `client/src/components/ContextMenu.tsx` | `client/src/lib/operationsApi.ts` | import togglePlugin | WIRED | `togglePlugin` imported from `@/lib/operationsApi` line 18 |
| `client/src/components/ArtifactDetailPanel.tsx` | `client/src/lib/operationsApi.ts` | import togglePlugin | WIRED | `togglePlugin` imported from `@/lib/operationsApi` line 15 |

### Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
|-------------|-------------|-------------|--------|----------|
| VIEW-01 | 03.1-01, 03.1-02 | Toggle between flat scope view and directory-hierarchy view | SATISFIED | View toggle in ArtifactTree, buildDirectoryItemMaps for hierarchy |
| VIEW-02 | 03.1-01, 03.1-02 | Non-Claude folders render as plain folder nodes | SATISFIED | `nodeKind: 'folder'` nodes with Folder icon in TreeItem |
| VIEW-03 | 03.1-02 | View preference persists across sessions via localStorage | SATISFIED | `cdt_view_mode` key read on mount, written on toggle |
| PLUG-01 | 03.1-01, 03.1-02 | User can click a toggle to enable/disable plugin, writing to settings.json | SATISFIED | Server route + client API + UI in ContextMenu and ArtifactDetailPanel. Ordering issue is UX, not functional. |
| PLUG-02 | 03.1-01, 03.1-02 | Plugin enabled/disabled state reflects current value in settings.json | SATISFIED | Plugin `enabled` field sourced from scanning (scanner reads enabledPlugins), badge rendered in TreeItem based on `data.enabled` |

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|------|------|---------|----------|--------|
| `client/src/components/tree/ArtifactTree.tsx` | 328 | `/ Flat default:` — malformed single-line comment (missing second `/`) | Info | Does not affect compilation (Vite build passes), but is a syntax oddity in a comment |

No stubs, placeholder returns, or TODO/FIXME markers found in phase-modified files.

### Human Verification Required

### 1. View Toggle Visual Correctness

**Test:** Start dev server. Look for two-icon toggle in the tree bar. Click FolderTree icon.
**Expected:** Tree switches to filesystem hierarchy. Non-Claude folders show Folder icon and muted text. Current project folder has blue "current" badge.
**Why human:** Visual appearance and expand/collapse behavior cannot be verified by static analysis.

### 2. Directory View Hierarchy Structure

**Test:** With multiple projects loaded, switch to directory view.
**Expected:** Global scope at top as separate root. Projects nest under shared filesystem path. Switching back to flat view restores scope grouping with section headers.
**Why human:** Requires runtime tree rendering and multi-scope fixture data.

### 3. Plugin Toggle End-to-End

**Test:** Find a plugin artifact. Right-click to open context menu.
**Expected:** Toggle action appears in context menu (currently NOT first — it appears after Open in Editor, Show in Folder, Copy Path). Toast fires on click. Tree refreshes. Enabled/disabled badge updates.
**Why human:** Requires running dev server with a plugin that has `enabled` field in settings.json.

### Gaps Summary

One gap blocks full goal achievement against the Plan 02 must_have truth: the plugin toggle action in the context menu is not positioned first before "Open in Editor". The PLAN explicitly stated "User can enable/disable a plugin via context menu action (first action before Open in Editor)" as both a must_have truth and as a required task action. The fix is a 6-line JSX reorder in `client/src/components/ContextMenu.tsx` — move the plugin toggle block before the ExternalLink row.

All data-layer work (Plan 01) is fully verified: tests pass, schemas exist, server route is correct, client API is wired. All UI wiring (Plan 02) is correct except the context menu ordering.

---

_Verified: 2026-03-30T23:05:00Z_
_Verifier: Claude (gsd-verifier)_
